import { AnyAction } from "redux"
import { update_substate } from "../../utils/update_state"

import { AppRootState } from "../state"



export const device_info_reducer = (state: AppRootState, action: AnyAction): AppRootState =>
{

    return state
}
